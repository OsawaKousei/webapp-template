FROM node:22-slim

RUN corepack enable && corepack prepare pnpm@latest --activate

WORKDIR /app/app

USER node

CMD ["sh", "-c", "set -e; mkdir -p .deps/node_modules; rm -rf node_modules; ln -s .deps/node_modules node_modules; pnpm install && pnpm dev"]